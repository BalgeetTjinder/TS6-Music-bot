# 🎵 Музыкальный сервер TeamSpeak 3 + SinusBot

Отдельный сервер для музыки с ботом SinusBot.

## Быстрый запуск

```bash
# 1. Создай папки для данных
mkdir -p ts3-data sinusbot-data sinusbot-scripts

# 2. Запусти контейнеры
sudo docker-compose up -d

# 3. Получи пароль администратора TS3
sudo docker logs ts3-music-server 2>&1 | grep -i "password\|token"

# 4. Получи пароль SinusBot
sudo docker logs sinusbot-music 2>&1 | grep -i password
```

## Порты

| Сервис | Порт | Назначение |
|--------|------|------------|
| TS3 сервер | 9988/udp | Голосовое подключение |
| TS3 Query | 10012 | Server Query |
| TS3 Files | 30034 | Файлы |
| SinusBot | 8087 | Веб-интерфейс |

## Подключение

### Пользователи подключаются к музыкальному серверу:
```
Адрес: твой_ip:9988
```

### Веб-интерфейс SinusBot:
```
http://твой_ip:8087
Логин: admin
Пароль: из логов (см. выше)
```

## Настройка SinusBot

1. Зайди в веб-интерфейс: `http://IP:8087`
2. Войди под admin
3. Перейди в **Settings** → **Instance Settings**
4. Измени адрес сервера на: `teamspeak3` (имя контейнера в Docker сети)
5. Порт: `9987` (внутренний порт)
6. Нажми **Save** и **Restart**

## Управление

```bash
# Посмотреть статус
sudo docker-compose ps

# Посмотреть логи
sudo docker-compose logs -f

# Перезапустить
sudo docker-compose restart

# Остановить
sudo docker-compose down
```

## Команды бота в TS3

После подключения бота, в чате сервера:
- `!play <URL>` — воспроизвести трек
- `!stop` — остановить
- `!pause` — пауза
- `!volume <0-100>` — громкость
- `!help` — список команд
