# TeamSpeak 6 Music Bot - Прототип

Минимальный прототип для проверки подключения к TeamSpeak 6 серверу.

## ⚠️ Важно

Этот прототип использует **Server Query API (SSH)** для проверки базового подключения к серверу.

**Server Query API** используется для:
- ✅ Административных задач (управление сервером, каналами, пользователями)
- ✅ Получения информации о сервере
- ❌ **НЕ предназначен для передачи аудио** (музыка)

Для музыкального бота потребуется **виртуальный клиент** (подключение как обычный пользователь), что может требовать другого подхода или библиотеки.

## 🚀 Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `config.json` на основе `config.example.json`:
```bash
cp config.example.json config.json
```

3. Заполните `config.json` вашими данными:
- `host` - IP адрес вашего TS6 сервера (92.255.207.41)
- `queryUsername` - имя пользователя для Server Query
- `queryPassword` - пароль для Server Query
- `nickname` - имя бота (пока не используется в прототипе)

## 📝 Как получить учетные данные Server Query

1. Подключитесь к серверу TeamSpeak 6 через клиент
2. Откройте настройки сервера
3. Найдите раздел "Server Query"
4. Создайте нового пользователя Query (если нет) или используйте существующего
5. Запишите имя пользователя и пароль

## 🏃 Запуск

```bash
npm start
```

Или с автоперезагрузкой (если установлен Node.js 18+):
```bash
npm run dev
```

## 🔍 Что проверяет прототип

1. ✅ Подключение к серверу через SSH
2. ✅ Получение списка виртуальных серверов
3. ✅ Подключение к первому серверу
4. ✅ Попытка получить список каналов

## 📊 Результаты тестирования

После запуска вы увидите:
- Успешно ли подключение
- Сколько найдено виртуальных серверов
- Работают ли базовые команды

## ⚠️ Ограничения

Этот прототип **НЕ** реализует:
- ❌ Виртуальный клиент (для музыки)
- ❌ Воспроизведение аудио
- ❌ Команды пользователей
- ❌ Управление каналами для музыки

## 🔄 Следующие шаги

1. **Если подключение работает:**
   - Проверить, есть ли в `teamspeak.js` поддержка Client API (виртуальный клиент)
   - Искать альтернативные библиотеки для виртуальных клиентов
   - Рассмотреть другие языки программирования (Python, и т.д.)

2. **Если подключение НЕ работает:**
   - Проверить настройки сервера (SSH Query включен?)
   - Проверить учетные данные
   - Проверить firewall
   - Попробовать другие библиотеки

## 📚 Полезные ссылки

- [teamspeak.js документация](https://teamspeak.js.org/)
- [TeamSpeak форум](https://community.teamspeak.com/)
- [TeamSpeak поддержка](https://support.teamspeak.com/)

## 📝 Лицензия

MIT