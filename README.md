# TS3AudioBot для TeamSpeak 6

Музыкальный бот для TeamSpeak 6 на базе [TS3AudioBot](https://github.com/Splamy/TS3AudioBot).

## Возможности

- 🎵 Воспроизведение музыки из YouTube, SoundCloud, прямых ссылок
- 📋 Очередь воспроизведения
- 🔊 Управление громкостью
- ⏭️ Пропуск, пауза, возобновление
- 🌐 Веб-интерфейс управления

## Установка

### 1. Сборка и запуск

```bash
# Собираем образ
sudo docker-compose build

# Запускаем
sudo docker-compose up -d

# Смотрим логи
sudo docker logs -f ts3audiobot
```

### 2. Первоначальная настройка

При первом запуске бот создаст конфигурационные файлы в папке `data/`.

Остановите бота и отредактируйте `data/ts3audiobot.toml`:

```bash
sudo docker-compose down
nano data/ts3audiobot.toml
```

Укажите параметры подключения к вашему TS6 серверу:

```toml
[bot.connect.default]
address = "127.0.0.1:9987"
name = "MusicBot"
```

### 3. Перезапуск

```bash
sudo docker-compose up -d
```

## Команды бота

В чате TeamSpeak:

- `!play <url>` — воспроизвести
- `!stop` — остановить
- `!pause` — пауза
- `!resume` — продолжить
- `!skip` — пропустить
- `!volume <0-100>` — громкость
- `!queue` — показать очередь
- `!help` — справка

## Веб-интерфейс

После запуска доступен по адресу: `http://ВАШ_IP:58913`
