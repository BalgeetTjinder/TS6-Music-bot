# TS3AudioBot для TeamSpeak 6

Музыкальный бот для TeamSpeak 6 на базе [TS3AudioBot](https://github.com/Splamy/TS3AudioBot) с патчем для поддержки TS6.

## Возможности

- 🎵 Воспроизведение музыки из YouTube, SoundCloud, прямых ссылок
- 📋 Очередь воспроизведения
- 🔊 Управление громкостью
- ⏭️ Пропуск, пауза, возобновление
- 🌐 Веб-интерфейс управления

## Установка (с патчем для TS6)

### 1. Установите зависимости

```bash
sudo apt update
sudo apt install -y dotnet-sdk-8.0 git libopus0
```

### 2. Соберите патченную версию

```bash
cd ~/TS6-Music-bot
chmod +x ts3audiobot-patch/build.sh
./ts3audiobot-patch/build.sh
```

### 3. Запустите бота

```bash
cd ~/TS3AudioBot/TS3AudioBot/bin/Release/net*/
./TS3AudioBot
```

При первом запуске:
- Введите ваш TeamSpeak UID (можно оставить пустым)
- Введите адрес сервера: `127.0.0.1:9987`

## Команды бота

В чате TeamSpeak:

- `!play <url>` — воспроизвести
- `!stop` — остановить
- `!pause` — пауза
- `!resume` — продолжить  
- `!skip` — пропустить
- `!volume <0-100>` — громкость
- `!queue` — показать очередь
- `!help` — справка

## Веб-интерфейс

После запуска доступен по адресу: `http://ВАШ_IP:58913`

## Патч для TS6

Патч добавляет поддержку:
- License block type 8 (новый в TS6)
- Key kind 7 (новый в TS6)

Файл патча: `ts3audiobot-patch/License.cs.patch`
