version: '3.8'

services:
  music-bot:
    build: .
    container_name: ts6-music-bot
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./config.json:/app/config.json:ro
      - ./cache:/app/cache
    environment:
      - NODE_ENV=production
