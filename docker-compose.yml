version: '3.8'

services:
  ts3audiobot:
    build: .
    container_name: ts3audiobot
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/opt/ts3audiobot/data
    environment:
      - TZ=UTC
