# SinusBot для TeamSpeak 6

SinusBot — готовое решение для музыкального бота TeamSpeak с веб-интерфейсом.

## 🚀 Быстрая установка

### 1. Перейдите в директорию sinusbot

```bash
cd sinusbot
```

### 2. Запустите SinusBot

```bash
docker-compose up -d
```

### 3. Получите пароль администратора

При первом запуске SinusBot генерирует пароль. Посмотрите логи:

```bash
docker logs sinusbot 2>&1 | grep -i password
```

Или:

```bash
docker logs sinusbot
```

Найдите строку типа:
```
Default login: admin / <password>
```

### 4. Откройте веб-интерфейс

Откройте в браузере:
```
http://IP_ВАШЕГО_СЕРВЕРА:8087
```

Войдите с логином `admin` и паролем из логов.

### 5. Настройте бота

1. Перейдите в **Settings** → **Instance Settings**
2. Добавьте сервер TeamSpeak:
   - **Hostname**: `127.0.0.1` (или IP вашего TS6 сервера)
   - **Port**: `9987` (голосовой порт)
   - **Query Port**: `10011`
   - **Query Username**: `serveradmin`
   - **Query Password**: ваш пароль
3. Настройте имя бота (Nickname)
4. Сохраните и запустите бота

## 📝 Команды в чате

SinusBot поддерживает команды в чате TeamSpeak:

| Команда | Описание |
|---------|----------|
| `!play <url>` | Воспроизвести с YouTube/Spotify |
| `!stop` | Остановить |
| `!pause` | Пауза |
| `!next` | Следующий трек |
| `!prev` | Предыдущий трек |
| `!vol <0-100>` | Громкость |
| `!queue` | Очередь |
| `!help` | Помощь |

## 🔧 Управление

### Просмотр логов
```bash
docker logs -f sinusbot
```

### Перезапуск
```bash
docker-compose restart
```

### Остановка
```bash
docker-compose down
```

### Обновление
```bash
docker-compose pull
docker-compose up -d
```

## ⚠️ Важно

- SinusBot использует **клиент TeamSpeak** для подключения к серверу
- Это позволяет **передавать аудио** в голосовые каналы
- Убедитесь, что порт 8087 (веб-интерфейс) доступен

## 🔗 Ссылки

- [Официальный сайт SinusBot](https://www.sinusbot.com/)
- [Документация](https://wiki.sinusbot.com/)
- [Docker Hub](https://hub.docker.com/r/sinusbot/docker)
